<style>

    * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Roboto', sans-serif;
     background: linear-gradient(135deg, #ea726b, #e0c76b);
     margin-top: 2%;
}

.container {
    width: 100%;
    max-width: 1200px;
    margin: 0 auto;
}

nav {
    background-color: #ff6347;
    padding: 20px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    color: white;
}

nav .logo {
    font-size: 24px;
    font-weight: 700;
}

nav ul {
    list-style: none;
    display: flex;
}

nav ul li {
    margin: 0 15px;
}

nav ul li a {
    color: white;
    text-decoration: none;
    font-weight: 500;
}

.profile {
    display: flex;
    gap: 20px;
}

section.reports {
    padding: 30px;
    color: #ffffff;
   
    
}

.reports h1 {
    font-size: 36px; /* Slightly larger font size */
    margin-bottom: 20px;
    margin-top: 20px;
    color: #ffffff; /* White color */
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7); /* Adds a shadow for a 3D effect */
    font-weight: bold;
    letter-spacing: 1.5px; /* Space out the letters slightly for elegance */
    background: linear-gradient(135deg, #ea726b, #e0c76b);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent; /* Makes text appear as a gradient */
}


.filters {
    display: flex;
    gap: 10px;
    margin-bottom: 20px;
   
}


.filters input {
    padding: 10px;
    font-size: 16px;
    border-radius: 10px;
    border: 3px solid #a15555;
     transition: transform 0.3s ease, box-shadow 0.3s ease; 
      background: linear-gradient(135deg, #ea726b, #e0c76b);
    
}
.filters button {
    padding: 10px;
    font-size: 20px; /* Increase the font size */
    border-radius: 10px;
    border: 3px solid #a15555;
    transition: transform 0.3s ease, box-shadow 0.3s ease; 
    background: linear-gradient(135deg, #ea726b, #e0c76b);
    color: rgb(8, 95, 0);
    cursor: pointer;
    border: none;
}


.filters input:hover{
    transform: translateY(-10px);
    box-shadow: 0 8px 15px rgba(0, 0, 0, 1);

}

.filters button:hover{
    transform: translateY(-10px);
    box-shadow: 0 8px 15px rgba(0, 0, 0, 1);

}

button {
    background-color: #28a745;
    color: rgb(8, 95, 0);
   
    cursor: pointer;
    border: none;
}

button:hover {
    background-color: #00ff59;
}

.summary-cards {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 20px;
    margin-bottom: 30px;
    
}

.card {
    background: linear-gradient(135deg, #ea726b, #e0c76b);
    padding: 20px;
    border-radius: 10px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.6);
    text-align: center;
    transition: transform 0.3s ease, box-shadow 0.3s ease; 
}
.card:hover{
    transform: translateY(-10px); 
    box-shadow: 0 8px 15px rgba(0, 0, 0, 1);
}

.card h2 {
    margin-bottom: 10px;
    font-size: 18px;
    font-weight: 500;
}

.card p {
    font-size: 24px;
    font-weight: 700;
    color: #ff6347;
}

.charts {
    display: flex;
    gap: 20px;
    
}

.chart {
    background: linear-gradient(135deg, #ea726b, #e0c76b);
    padding: 20px;
    border-radius: 10px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.6);
    flex: 1;
    transition: transform 0.3s ease, box-shadow 0.3s ease; 
}
.chart:hover{
    transform: translateY(-10px);
    box-shadow: 0 8px 15px rgba(0, 0, 0, 1);
}


</style> 
 
 
 <section class="reports"></section>
            <h1>Reports(Comming Soon)</h1>

            <!-- Filter Section -->
            <div class="filters">
                <input type="date" id="startDate" name="startDate">
                <input type="date" id="endDate" name="endDate">
                <button>Generate Report</button>
            </div>

            <!-- Summary Cards -->
            <div class="summary-cards">
                <div class="card">
                    <h2>Total Sales</h2>
                    <p>₹120,000</p>
                </div>
                <div class="card">
                    <h2>Total Orders</h2>
                    <p>650</p>
                </div>
                <div class="card">
                    <h2>Active Customers</h2>
                    <p>200</p>
                </div>
                <div class="card">
                    <h2>Top Product</h2>
                    <p>Burger</p>
                </div>
            </div>

            <!-- Charts Section -->
            <div class="charts">
                <div class="chart">
                    <h3>Sales Over Time</h3>
                    <canvas id="salesChart"></canvas>
                </div>
                <div class="chart">
                    <h3>Order Trends</h3>
                    <canvas id="ordersChart"></canvas>
                </div>
            </div>
        </section>